interface:
  display_name: "UI Design Workbench"
  short_description: "Platform-aware UI workbench and guidance"
  default_prompt: "Use $ui-design-workbench for this repository UI task, following its optional project guidance mode and the matching platform conventions without expanding the requested scope."
